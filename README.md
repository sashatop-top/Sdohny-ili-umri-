
# Игра "Сдохни или умри"
Есть поле размером NxM (указывается с клавиатуры), на котором расположены враги, бонусы и оружие. Цель: дойти до конечной точки при этом не сдохнуть и не умереть. 😌

## Классы
### NPC
`Игрок`<br>
С собой игрок носит все виды бонусов и только **одно** оружие.

`Крыса`<br>
Во время боя с вероятностью в 25% крыса может заразить игрока на 3 хода. В этом случае перед ходом игрока с него дополнительно снимается `5 * (1 + lvl / 10)`. С вероятностью в 10% при низком уровне здоровья (ниже 15%) крыса может убежать. Если крыса сбежала, то бой заканчивается. За победу даётся 200 монет.

`Паук`<br>
Во время боя с вероятностью в 10% паук может отравить игрока на 2 хода. В этом случае перед ходом игрока с него дополнительно снимается `15 * (1 + lvl / 10)`. С вероятностью в 10% при низком уровне здоровья (ниже 15%) паук может призвать другого паука. При этом текущий паук не убегает с поля боя. Призванный паук также может призвать нового пука. За победу над каждым пауком даётся 250 монет в конце боя.

`Скелет`<br>
Скелет может атаковать оружием. За победу даётся 150 монет и оружие, которое было у скелета, кроме кулака.

### Оружие
В инвентаре может быть только 1 оружие. Самостоятельно сменить оружие нельзя. Сменить дополнительное оружие на другое можно только тогда, когда будет найдено новое оружие. Если палка сломалась, закончились стрелы или патроны, то смена на кулак происходит автоматически. Если игрок попал на клетку с оружием, то у игрока есть выбор сменить оружие или нет.

`Кулак`<br>
Стартовое оружие. Максимальный урон: 20.

`Палка`<br>
Максимальный урон: 25. Имеет уровень прочности. После того, как прочность оказалась на нуле, то палка ломается.

`Лук`<br>
Максимальный урон: 35. Стрелять можно только стрелами. Если стрелы закончились, то лук становится недоступен. Лук расходует на выстрел 1 стрелу.

`Револьвер`<br>
Максимальный урон: 45. Стрелять можно только патронами. Если патроны закончились, то револьвер становится недоступен. Револьвер расходует на выстрел 2 патрона.

### Бонусы
Бонусы собираются автоматически.

`Аптечка`<br>
Увеличивает здоровье, но не выше максимального. Одноразовое применение. Сила аптечки определяется случайным значением от 10 до 40 в момент создания объекта. Стоимость аптечки - 75 монет.

`Ярость`<br>
Увеличивает урон кулаком и палкой. Одноразовое применение. Сила ярости определяется случайным значением от 0.1 до 1.0 в момент создания объекта. Стоимость ярости - 50 монет.

`Стрелы`<br>
Увеличивает кол-во стрел. Кол-во стрел определяется случайным значением от 1 до 20 в момент создания объекта. Нельзя купить.

`Патроны`<br>
Увеличивает кол-во патронов. Кол-во патронов определяется случайным значением от 1 до 10 в момент создания объекта. Нельзя купить.

`Точность`<br>
Повышает точность стрельбы из лука и револьвера. Сила точности определяется случайным значением от 0.1 до 1.0 в момент создания объекта. Стоимость - 50 монет.

`Монеты`<br>
Увеличивает кол-во монет. Кол-во монет определяется случайным значением от 50 до 100 в момент создания объекта.

### Строения
`Башня`<br>
Открывает на поле соседние клетки радиусом 2.

### Поле
Содержит в себе информацию кто где находится.

### Ход игры
Начинает в клетке (1, 1), его задача добраться до клетки (N, M). Управление происходит с клавиатуры, путём указания направления движения. Выходить за пределы поля нельзя.

По ходу движения игрок может столкнуться с врагами. В этом случае происходит пошаговый бой. Игрок ходит первым. После победы игрок получает монеты. Во время боя игрок может использовать бонусы в дополнение к атаке. Если бонусов нет, то бонус автоматически покупается за монеты. Если бой закончился, а у игрока остался статус заражения/отравления, то перед переходом на другую клетку также снимаются очки здоровья. Иными словами: ход игрока - это атака или перемещение.

Взаимодействие с инвентарем происходит через клавиатуру. 

При передвижении отрисовывается поле, на котором указываются закрытые, открытые ячейки, а также метками E, B, T, P указыватся враг, бонус, башня и игрок соответственно.

Пример поля:
```
-------------
| |W|X|X|X|X|
|T| |B| |X|X|
|E| |X|P|X|X|
-------------
```
При создании врагов им назначается случаным образом уровень от 1 до 10 случайным образом. Кол-во здоровья у них определяется по формуле: `100 * (1 + lvl / 10)`. Максимальный урон определяется по формуле:
- крыса: `15 * (1 + lvl / 10)`
- паук: `20 * (1 + lvl / 10)`
- скелет: `10 * (1 + lvl / 10)`
Враг бьёт, нанося случаный урон от 0 до максимального урона.

В момент создания игрока указывается уровень. Кол-во здоровья у игрока определяется по формуле: `150 * (1 + lvl / 10)`. Точность и ярость изначально равны 1.

Урон игрока определяется так: 
- для стрелкового оружия: генерируется случайное число от 0 до максимального урона, далее это число умножается на коэффициент точности - полученное значение и есть урон игрока.
- для ударного оружия: генерируется случайное число от 0 до максимального урона, далее это число умножается на коэффициент ярости - полученное значение и есть урон игрока.
Урон не может быть выше максимально допустимого.

Написать функцию `game`, в которой реализовать основную логику игры. Выводить всю изменяемую информацию: поле, сколько урона нанесено, сколько здоровья осталось, как изменился инвентарь, сколько монет потрачено и т.д. В функции `start` реализовать первоначальное создание объектов и их инициализацию.

Функции `start` и `game` вызвать так:
```
if __name__ == "__main__":
    start()
    game()
```
Реализовать цветной вывод информации. Как минимум, реализовать вывод сообщения о том, что находится на клетке. Красным - враг, зеленым - бонус/оружие, оранжевым - башня.

---

## Спецификация классов

Ниже приведены **абстрактные родительские классы** и конкретные классы проекта с перечнем атрибутов и методов. Инкапсуляцию (public/protected/private) определеяете самостоятельно.

### Базовые абстракции

#### `class Entity(ABC)`
**Назначение:** общий интерфейс для всего, что может находиться на поле.
- **Атрибуты:**
  - `position: tuple[int, int]` — координаты на поле `(row, col)`.
- **Методы:**
  - `@abstractmethod
  def symbol(self) -> str` — символ для отрисовки на поле.

#### `class Damageable(ABC)`
**Назначение:** кто может получать урон/исцеление.
- **Атрибуты:**
  - `hp: float` — текущее здоровье.
  - `max_hp: float` — максимум здоровья.
- **Методы:**
  - `def is_alive(self) -> bool` — жив ли объект.
  - `def heal(self, amount: float) -> float` — восстановить здоровье, вернуть фактически восстановленное значение.
  - `def take_damage(self, amount: float) -> float` — получить урон, вернуть фактически нанесённый урон.

#### `class Attacker(ABC)`
**Назначение:** кто может наносить урон.
- **Методы:**
  - `@abstractmethod
def attack(self, target: Damageable) -> float` — совершить атаку и вернуть нанесённый урон.

#### `class Bonus(Entity)`
**Назначение:** абстракция всех бонусов.
- **Методы:**
  - `@abstractmethod
def apply(self, player: 'Player') -> None` — применить бонус к игроку.
  - `def symbol(self) -> str`

#### `class Weapon(Entity)`
**Назначение:** абстракция оружия.
- **Атрибуты:**
  - `name: str` — название.
  - `max_damage: float` — максимальный урон «сырым» ударом/выстрелом.
- **Методы:**
  - `@abstractmethod
def roll_damage(self) -> float` — сгенерировать «сырое» значение урона от 0 до `max_damage` (конкретную реализацию написать в классах `Fist`, `Stick` и т.д.).
  - `def is_available(self) -> bool` — доступно ли к использованию (боезапас/прочность).
  - `def symbol(self) -> str`

#### `class MeleeWeapon(Weapon)`
**Назначение:** ударное оружие, масштабируется яростью.
- **Методы:**
  - `def damage(self, rage: float) -> float` — итоговый урон с коэффициентом ярости.

#### `class RangedWeapon(Weapon)`
**Назначение:** стрелковое оружие, масштабируется точностью.
- **Атрибуты:**
  - `ammo: int` — текущий боезапас.
  - `ammo_consumption` - расход боеприпасов
- **Методы:**
  - `def consume_ammo(self, n: int) -> bool` — попытаться потратить `n` боеприпасов.
  - `def damage(self, accuracy: float) -> float` — итоговый урон с учётом точности, если есть боезапас.

#### `class Structure(Entity)`
**Назначение:** абстракция построек на поле.
- **Методы:**
  - `@abstractmethod
def interact(self, player: 'Player') -> None` — действие при входе игрока.
  - `def symbol(self) -> str`

#### `class Enemy(Entity, Damageable, Attacker)`
**Назначение:** абстракция врагов.
- **Атрибуты:**
  - `lvl: int` — уровень (1–10).
  - `max_enemy_damage: float` — максимум урона врага.
  - `reward_coins: int` — монеты за победу.
- **Методы:**
  - `@abstractmethod
def before_turn(self, player: 'Player') -> None` — триггеры статусов/призыва перед ходом игрока (яд/инфекция и т.д.).
  - `def roll_enemy_damage(self) -> float` — случайный урон врага от 0 до `max_enemy_damage`.
  - `def symbol(self) -> str`

---

### Конкретные классы

#### `class Player(Entity, Damageable, Attacker)`
**Родитель:** `Entity`, `Damageable`, `Attacker`  
- **Атрибуты:**
  - `lvl: int` — уровень игрока.
  - `weapon: Weapon` — текущее оружие (всегда одно).
  - `inventory: dict[str, list[Bonus]]` — инвентарь (монеты, стрелы, патроны и т.д.).
  - `coins: int` - кол-во монет
  - `rage: float` — коэффициент ярости (≥ 1.0 по умолчанию 1.0).
  - `accuracy: float` — коэффициент точности (≥ 1.0 по умолчанию 1.0).
  - `statuses: dict[str, int]` — активные статусы с оставшимися ходами (например, `{"poison": 2, "infection": 3}`).
  - `fight: bool` — статус в бою ли игрок
- **Методы:**
  - `def move(self, d_row: int, d_col: int) -> None` — попытка перемещения.
  - `def attack(self, target: Damageable) -> float` — атака текущим оружием с учётом ярости/точности, возвращает урон.
  - `def choose_weapon(self, new_weapon: Weapon) -> None` — выбор заменить ли оружие при поднятии.
  - `def apply_status_tick(self) -> float` — применить урон от статусов перед своим ходом, вернуть нанесённый себе урон.
  - `def add_coins(self, amount: int) -> None` — пополнить монеты.
  - `def use_bonus(self, bonus: Bonus) -> None` — применить бонус (если есть).
  - `def buy_auto_if_needed(self, bonus: str) -> Bonus` — автопокупка нужного бонуса, если он требуется в бою.
  - `def symbol(self) -> str`
  - `def change_fight(self) -> None` - изменение статуса "в бою" на противоложный

#### Враги

##### `class Rat(Enemy)`
- **Атрибуты:**
  - `infection_chance: float = 0.25`
  - `flee_chance_low_hp: float = 0.10`
  - `flee_threshold: float = 0.15` — доля HP, при которой возможен побег.
  - `infection_damage_base: float = 5.0`
  - `infection_turns: int = 3`
  - `reward_coins: int = 200`
- **Методы:**
  - `def before_turn(self, player: Player) -> None` — шанс инфицировать игрока, шанс сбежать при низком HP.
  - `def attack(self, target: Damageable) -> float` — урон от 0 до `15 * (1 + lvl / 10)`.

##### `class Spider(Enemy)`
- **Атрибуты:**
  - `poison_chance: float = 0.10`
  - `summon_chance_low_hp: float = 0.10`
  - `poison_damage_base: float = 15.0`
  - `call_threshold: float = 0.15` — доля HP, при котором призывается дополнительный паук.
  - `poison_turns: int = 2`
  - `reward_coins: int = 250`
- **Методы:**
  - `def before_turn(self, player: Player) -> None` — шанс отравить, шанс призвать паука при низком HP (не убегает).
  - `def attack(self, target: Damageable) -> float` — урон от 0 до `20 * (1 + lvl / 10)`.

##### `class Skeleton(Enemy)`
- **Атрибуты:**
  - `weapon: Weapon` — оружие скелета (может быть «Кулак»).
  - `reward_coins: int = 150`
- **Методы:**
  - `def before_turn(self, player: Player) -> None` — специальных эффектов нет.
  - `def attack(self, target: Damageable) -> float` — наносит урон своим оружием (не расходует ресурс оружия).
  - `def drop_loot(self, player: Player) -> Weapon | None` — вернуть оружие, если оно не «Кулак».

#### Оружие

##### `class Fist(MeleeWeapon)`
- **Атрибуты:** 
  - `name = "Кулак"`
  - `max_damage: float = 20`
- **Методы:** 
  - `damage(self, rage: float) -> float`

##### `class Stick(MeleeWeapon)`
- **Атрибуты:**
  - `name = "Палка"`
  - `max_damage: float = 25`
  - `durability: int` — прочность; случайное значение от 10 до 20.
- **Методы:**
  - `def is_available(self) -> bool`
  - `def damage(self, rage: float) -> float` — уменьшает `durability` на 1; когда 0 — ломается.

##### `class Bow(RangedWeapon)`
- **Атрибуты:** 
  - `name = "Лук"`
  - `max_damage: float = 35`
  - `ammo: int` — стрелы; случайное значение от 10 до 15.
- **Методы:**
  - `def is_available(self) -> bool` — доступен только при `ammo > 0`.
  - `def damage(self, accuracy: float) -> float` — расходует 1 стрелу, возвращает урон.

##### `class Revolver(RangedWeapon)`
- **Атрибуты:** 
  - `name = "Револьвер"`
  - `max_damage: float = 45`
  - `ammo: int` — патроны; случайное число от 5 до 10.
- **Методы:**
  - `def is_available(self) -> bool`
  - `def damage(self, accuracy: float) -> float` — расходует 2 патрона, возвращает урон.

#### Бонусы

##### `class Medkit(Bonus)`
- **Атрибуты:**
  - `power: int` — от 10 до 40 при создании.
- **Методы:**
  - `def apply(self, player: Player) -> None` — лечит, если применяется в бою, но не выше `max_hp`, иначе добавляется в инвентарь.

##### `class Rage(Bonus)`
- **Атрибуты:**
  - `multiplier: float` — от 0.1 до 1.0 при создании (прибавка к коэффициенту ярости).
- **Методы:**
  - `def apply(self, player: Player) -> None` — увеличивает `player.rage` на `multiplier` (одноразово), если в бою, иначе помещается в инвентарь.

##### `class Arrows(Bonus)`
- **Атрибуты:** 
  - `amount: int` — 1..20 при создании; **нельзя купить**.
- **Методы:**
  - `def apply(self, player: Player) -> None` — если у игрока лук, увеличить `ammo`; иначе пополнить запас стрел в инвентаре.

##### `class Bullets(Bonus)`
- **Атрибуты:** 
  - `amount: int` — 1..10 при создании; **нельзя купить**.
- **Методы:**
  - `def apply(self, player: Player) -> None` — аналогично стрелам для револьвера.

##### `class Accuracy(Bonus)`
- **Атрибуты:** 
  -`multiplier: float` — 0.1..1.0; `price: int = 50`.
- **Методы:**
  - `def apply(self, player: Player) -> None` — увеличивает `player.accuracy` на `multiplier` (одноразово), если в бою, иначе помещается в инвентарь.

##### `class Coins(Bonus)`
- **Атрибуты:** 
  - `amount: int` — 50..100.
- **Методы:**
  - `def apply(self, player: Player) -> None` — добавить монеты.

#### Постройки

##### `class Tower(Structure)`
- **Атрибуты:** 
  - `reveal_radius: int = 2`
- **Методы:**
  - `def interact(self, board: Board) -> None` — открыть клетки вокруг башни (в 8 направлениях) в радиусе `reveal_radius`.

#### Поле боя

##### `class Board`
- **Атрибуты:**
  - `rows: int`, `cols: int` — размеры `N x M`.
  - `grid: list[list[tuple[Entity | None, bool]]` — содержимое клеток, кортеж из объекта и видимости клетки.
  - `start: tuple[int, int] = (0, 0)` — стартовая клетка.
  - `goal: tuple[int, int] = (N-1, M-1)` — конечная клетка.
- **Методы:**
  - `def place(self, entity: Entity, pos: tuple[int, int]) -> None` — разместить сущность.
  - `def entity_at(self, pos: tuple[int, int]) -> Entity | None` — получить сущность в клетке.
  - `def in_bounds(self, pos: tuple[int, int]) -> bool` — в пределах ли поля.
  - `def render(self, player: Player) -> str` — вернуть текстовую отрисовку с символами `E,B,T,P,X,W` и открытыми клетками.

#### Инициализация и цикл игры

- `def start(n: int, m: int, player_lvl: int) -> tuple[Board, Player]` — создать поле, сгенерировать башни (≈1% от общего числа клеток), оружие (≈5%, кроме кулака), бонусы (≈30%), врагов (≈15%), гарантировать пустые старт/финиш.
- `def game(board: Board, player: Player) -> None` — основной цикл ввода, перемещения, боёв и отрисовки.
- `def main() -> None` — точка входа, вызывает `start` и `game()`.

### Обозначения на карте (символы)
- Игрок — `P`, Враг — `E`, Бонус — `B`, Башня — `T`, Закрытая клетка — `X`, Оружие - `W`, Пусто — пробел.